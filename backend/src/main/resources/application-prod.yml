spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/maint
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:maint}
    password: ${DB_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.maint: INFO
